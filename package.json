{
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "vite": "^2.3.5"
  },
  "dependencies": {
    "jest": "^277.0.3"
  }
}
